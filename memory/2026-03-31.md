# Daily Note — 2026-03-31

## SpeedShield — Field Test + Production Hardening

### What happened
Alistair took the app to the field and ran real tests. TTS alerts fired at correct distances, directional filtering confirmed working. This closed out the core alert loop as validated — the app works as intended in real conditions.

### Key developments (from project file)
1. **Field test passed** — Alerts fire at 600m and 100m with correct directional filtering
2. **Battery optimization exemption** — Android Doze was killing the background service; REQUEST_IGNORE_BATTERY_OPTIMIZATIONS added to manifest + runtime request
3. **Bluetooth detection rewritten in native Kotlin** — FlutterBluePlus was unreliable in background Dart isolate; replaced with native BroadcastReceiver listening to ACL_CONNECTED/DISCONNECTED events, writing to SharedPreferences; Dart reads from there
4. **Doze-aware smart activation** — Full architecture:
   - `BluetoothReceiver` (Kotlin) handles BT connect/disconnect, starts/stops Flutter service — Doze-exempt
   - `ActivityRecognitionReceiver` (Kotlin, Google Play Services IN_VEHICLE) handles auto-detect — Doze-exempt, 30s updates
   - `MonitoringService` bridges native → Flutter
   - Result: zero idle battery in vehicleBluetooth mode
5. **BT name save snackbar** — Confirmation feedback added; was saving silently before

### Status at day's end
App is production-grade on Android. Background monitoring is robust, Doze-aware, battery-efficient. The alert loop is field-validated. This is effectively MVP-complete for Android.

---

## Nightly Self-Improvement Run — 2026-03-31 (11:00 AM CET)

### Consolidation Check
Last consolidation: 2026-03-28. New daily notes since: 03-29, 03-30, 03-31 = 3 days since last run. Protocol threshold is >3 days — not triggered tonight, but consolidation should run tomorrow (04-01) if another daily note accumulates.

### Improvement Made
**Fixed factual errors in civic-speed-monitor.md tech stack table:**
- Maps: corrected `google_maps_flutter` → `flutter_map + OpenStreetMap tiles` (changed weeks ago; stale entry would mislead future sessions)
- Bluetooth: corrected `flutter_blue_plus` → native Kotlin BroadcastReceiver architecture (changed today; table must reflect current build)
- Added Activity Recognition and MonitoringService rows (new native components)

These were factual errors, not style nits — a session loading the project file tomorrow would have read an incorrect tech stack.

**MEMORY.md updated** — SpeedShield status refreshed to reflect field test pass and Doze-aware activation. v1.6.

### Key Lessons / Decisions
- Once a Flutter plugin is replaced with native code, update the tech stack table *in the same turn* — stale entries compound across sessions
- Android Doze is a first-class constraint for any location/BT app; native BroadcastReceivers are Doze-exempt, Flutter Dart isolates are not
- FlutterBluePlus is reliable in foreground; unreliable in background isolates — use native ACL events for production BT detection
- Google Play Services ActivityRecognition (IN_VEHICLE activity type) provides clean vehicle-motion detection without GPS drain; ~30s update interval is the minimum

---

## Open Items (SpeedShield)
- [ ] Deploy fixed update_cameras.py to Phoenix-Speed-Cameras GitHub repo
- [ ] Codemagic account setup (codemagic.io → connect SpeedShield repo)
- [ ] In-app purchase layer ($4.99 one-time + $9.99/yr renewal)
- [ ] Google Play Console account — Alistair action
- [ ] Apple Developer account — Alistair action
- [ ] iOS build via Codemagic + App Store submission

## Open Items (Other)
- [ ] ServiceVoice: validation survey not yet built
- [ ] Healthcare Guardian: duplicate A record (162.255.119.162) — Alistair DNS cleanup
- [ ] Fueled for Duty: blocked on HeyGen API key, Avatar ID, Voice ID
- [ ] Contabo VPS snapshot still deferred
